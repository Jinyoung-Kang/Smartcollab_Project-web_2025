spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartcollabdb?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

file:
  storage:
    mode: local
    local-root: ${user.home}/smartcollab_uploads
jwt:
  secret: dev-secret
  expiration-hours: 3
